<div class="container">
  <h1>Display products</h1>
  <div class="row">
    <% @products.each do |product| %>
      <div class="card-trip m-3 col-sm">
        <%= link_to product_path(product), class: "text-decoration-none text-reset" do %>
          <%= cl_image_tag product.photo_id.to_s, height: 300, width: 400, crop: :fill %>
          <div class="card-trip-infos">
            <div>
              <h2><%= product.name %></h2>
              <p>in stock: <%= product.stock_quantity %></p>
            </div>
            <h2 class="card-trip-pricing"><%= product.price%>$</h2>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
<div style="width: 100%; height: 500px;"
  data-controller="map"
  data-map-markers-value="<%= @markers.to_json %>"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
</div>
